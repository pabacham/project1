// Filename: app.js
define([

], function(){

    var config = {
        views: {},
        models: {},
        templates: {},
        socket: null,
        config: {
            serviceUrl: window.location.origin+ "/api/"
        }
    };

    return config;
});


